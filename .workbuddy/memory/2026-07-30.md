# 2026-07-30 工作日志

## Signal 项目 - 批次0 脚手架

### 技术栈定稿
- Tauri 2 + React 19 + TypeScript + Vite 7 + Tailwind 3 + shadcn/ui 风格
- Rust 后端：reqwest/tokio/sqlx/scraper/chrono 等
- 状态管理：Zustand；虚拟滚动：@tanstack/react-virtual

### 已完成
- `npm create tauri-app` 创建项目，rsync 复制到 `/Users/yyy/Desktop/Signal`（保留效果图 PNG）
- 重命名 signal-tmp → signal（package.json / Cargo.toml / tauri.conf.json / main.rs / lib name）
- 插件：tauri-plugin-opener + tauri-plugin-store
- **DB 层决策**：用 sqlx 直接在 Rust 端操作，**不用 tauri-plugin-sql**（避免版本冲突，前端通过 invoke 命令访问 DB）
- Tailwind 配置 + 设计 tokens 固化到 `tailwind.config.js`（bg/panel/line/ink/muted/faint/accent/accent-soft 颜色，card/btn 圆角）
- `src/styles/globals.css`（Tailwind 指令 + 自定义滚动条）
- `src/lib/utils.ts`（cn 函数 + formatRelativeTime）
- 窗口 1200×800，最小 1024×640
- `npm install` 成功（154 包，0 漏洞）
- `git init` + 初始提交 `chore: init project scaffold` + branch main + remote origin → `git@github.com:YYY2579/Signal.git`

### 进行中
- `cargo check` 验证 Rust 编译（首次下载依赖，后台运行 task_id=W434il）

### 待完成
- cargo check 通过后 `git push -u origin main`
- 验证 `npm run tauri dev`（可选，cargo build 已隐含验证）

### 关键决策
- React 19（模板默认，非计划里的 18，兼容无碍）
- 批次0 lib.rs 最小化（只注册 opener + store 插件），mod 声明留给批次1
- capabilities/default.json 权限：core + opener + store（无 sql，因为不用 tauri-plugin-sql）
